# El siguiente archivo es un ejemplo de configuración de Prometheus para monitorear una API de SmartBancs.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "smartbancs-api"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:8000"